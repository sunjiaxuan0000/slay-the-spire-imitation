# 单元测试文档

> 项目：卡牌 roguelike 游戏（JavaFX + Maven，JDK 17）
> 编写日期：2026-09-14
> 测试框架：JUnit 5（Jupiter 5.12.1）+ maven-surefire-plugin 3.2.5

---

## 1. 测试范围与策略

本轮单元测试聚焦**不依赖 JavaFX 运行时的纯逻辑层**，这些类只使用 `java.util`，可在无头（headless）环境直接断言，测试稳定、毫秒级完成：

| 模块 | 被测类 | 是否覆盖 |
|---|---|---|
| Enemy | `Enemy` 基类 + 全部 10 个子类（含 4 组静态工厂变体） | ✅ |
| Enemy | `EnemyFactory`（楼层概率池、读档重建、种子确定性） | ✅ |
| Character | `Player`（生命/上限/牌组/遗物） | ✅ |
| View（模型部分） | `GameMap.ROWS` 等常量被工厂测试间接引用 | ✅ 间接 |
| Battle | `BattleView`（JavaFX 节点、动画、弹层） | ❌ 见 §7 |
| Card/View UI | 卡面渲染、地图渲染、HUD | ❌ 见 §7 |

**测试策略**：
- 数值、轮盘顺序、状态机切换等**确定性逻辑**：精确断言；
- 概率分布：大样本统计抽样（每层 5000~6000 次）+ 容差区间断言；
- 随机机制（闪电猪闪避、混沌猪增益）：前者统计频率，后者验证同种子序列一致。

---

## 2. 运行方式

```powershell
# 运行全部单元测试
.\mvnw.cmd test

# 只跑单个测试类
.\mvnw.cmd test -Dtest=EnemyMechanicsTest
```

测试源码位于 `src/test/java/`，与主代码同包结构。原始执行报告由 surefire 自动生成在：

```
target/surefire-reports/
├── *.txt          人类可读汇总
└── *.xml          结构化结果（CI 可用）
```

---

## 3. 测试文件与用例清单（共 47 个）

### 3.1 EnemyMechanicsTest —— 敌人机制（26 个）

路径：`src/test/java/com/example/demo/enemy/EnemyMechanicsTest.java`

| 分组 | 用例 | 验证内容 |
|---|---|---|
| 基类默认 | baseDefaults | 无闪避/无持久护甲/不削上限/无反伤/无仪式的默认值 |
| 基类默认 | defaultBuffAndDexterity | 默认 BUFF = 力量 +N；敏捷加成格挡（+6 后 5→11） |
| 史莱姆 | baseStats | 36 血；攻击8/防御5/虚弱2；立绘 210；攻击塞 1 黏液 |
| 史莱姆 | wheelCycles | 三步轮盘执行后正确回到起点 |
| 史莱姆 | bigStats | 大史莱姆 68 血/攻16/防14；复用「史莱姆」立绘；贴图 250 |
| 史莱姆 | intentTextAttack | 意图文字含「攻击 8」，加 2 力量后变「攻击 10」 |
| 邪教猪 | baseRitualWheel | 45 血；首回合仪式 2，之后攻击 6 永久循环 |
| 邪教猪 | veteranStats | 老兵 60 血/攻 8；复用邪教猪立绘；贴图 170 |
| 邪教猪 | ritualTiming | **仪式时序**：激活当回合 0 力量 → 下回合跳过 → 之后每回合 +2 |
| 神风猪 | planAndPerStackDamage | 56 血；攻10/蓄1/蓄1；每层自爆 16 |
| 神风猪 | explodeDamageByStacks | 自爆伤害 = 层数×16（0/16/48） |
| 神风猪 | deathLockFlow | 首次归 0 锁血成功且 current() 变 EXPLODE；二次拦截失败 |
| 卫士猪 | guardPigStats | 80 血精英、30% 反伤、20/反伤2/防20/18 四步循环 |
| 闪电猪 | stats | 70 血精英、hasDodge=true、UI 文案概率 40% |
| 闪电猪 | dodgeRateStatistical | 20000 次闪避频率落在 35%~45%（期望 40%） |
| 巨猪骑士 | baseStats | 15 血/500 护甲；BOSS；护甲持久；削血上限；立绘 300 |
| 巨猪骑士 | armorDecay | 回合开始自损当前护甲 4%（500→480→461；1 点护甲保持 1） |
| 巨猪骑士 | attackFormula | 破甲前 `5+损甲×3%`、破甲后 `10+损甲×2%`，250 为切换点 |
| 猪龙鱼公爵 | phaseOne | 200 血；满血不转阶段 |
| 猪龙鱼公爵 | phaseByHp | HP>100 不转；HP≤100 转二阶段并重置为攻击18轮盘 |
| 猪？ | phaseOneStats | 100 血普通怪（isBoss=false）；攻12/防20/吐黏液3/防30 |
| 猪？ | phaseByTurnNotHp | HP=1 不转；前 4 回合不转；第 5 回合后转攻击30轮盘 |
| 海兵猪 | seaSoldierStats | 45 血；防20/潮湿1/攻12/强化3；立绘 180 |
| 混沌猪 | wheelLoop | 前两步播一次后从「防御12」处进入 3 步循环 |
| 混沌猪 | seedDeterminism | 同战斗种子两次生成的增益抽签序列完全一致 |
| 混沌猪 | noDodgeBeforeBuff | 未抽到闪避增益前 hasDodge/dodge 恒 false |

### 3.2 EnemyFactoryTest —— 工厂与概率（13 个）

路径：`src/test/java/com/example/demo/enemy/EnemyFactoryTest.java`

| 用例 | 验证内容 |
|---|---|
| earlyRowsOnlyBasePool | row 0~4 只产出史莱姆/邪教猪/海兵猪，且三种都能刷到 |
| earlyRowRatios | 基础池内部 40%/30%/30%（6000 样本，容差 ±4.5%） |
| advancedPoolRateByRow | row5 高级池 60%、row16 高级池 80%（容差 ±4%） |
| mysteryPigOnlyLateRows | 猪？在 row 5~11 出现 0 次；row13 可出现 |
| midAdvancedPoolRatios | row8 高级池内部 大史莱姆40/老兵30/神风30（条件分布，容差 ±6%） |
| lateAdvancedPoolRatios | row16 高级池内部 20/30/10/40（条件分布，容差 ±5%） |
| flagsBySource | 普通怪 isElite/isBoss 均 false；精英/BOSS 标记正确 |
| namedRoundTrip | 12 个名字逐个精确重建为对应类（读档契约） |
| namedPreservesVariantStats | named 重建保留变体数值（大史莱姆 68 血/250，老兵 60 血/170） |
| namedFallback | null/空串/未知名字退回随机生成且不返回 null |
| eliteDeterministic | 同种子两次生成同一只精英（防 SL 刷怪） |
| eliteCoversAllThree | seed 0~999 覆盖卫士猪/闪电猪/混沌猪三种精英 |
| bossByKind | 指定 BOAR/DUKE 必出对应 BOSS；null 退随机 |

### 3.3 PlayerTest —— 玩家状态（8 个）

路径：`src/test/java/com/example/demo/character/PlayerTest.java`

| 用例 | 验证内容 |
|---|---|
| initialState | 80/80 血、100 金币、初始燃烧之血、10 张起始牌（5打击/4防御/1痛击） |
| damageClampsToZero | 伤害不会扣成负数 |
| healClampsToMax | 治疗不会超过上限 |
| reduceMaxHpClampsHp | 削上限时当前血夹到新上限；上限可减到 0（巨猪骑士致死路径） |
| reduceMaxHpIgnoresNonPositive | 0/负数削减无效 |
| increaseMaxHp | 上限与当前血同步增加，残血时不回满 |
| addRelicDeduplicates | 同名遗物不重复；null 安全 |
| thermosRelicEffect | 保温杯获得即 +8 最大生命（88/88） |

---

## 4. 执行结果（真实执行证据）

执行命令：`mvn test`（maven-surefire-plugin 3.2.5）
执行日期：2026-09-14

```
Tests run: 47, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

分类明细（摘自 `target/surefire-reports/*.txt`）：

| 测试类 | 用例数 | 通过 | 失败 | 错误 | 跳过 |
|---|---|---|---|---|---|
| PlayerTest | 8 | 8 | 0 | 0 | 0 |
| EnemyFactoryTest（含 SeedTests） | 13 | 13 | 0 | 0 | 0 |
| EnemyMechanicsTest（含 8 个嵌套分组） | 26 | 26 | 0 | 0 | 0 |
| **合计** | **47** | **47** | **0** | **0** | **0** |

**通过率 100%**。全部测试为纯内存计算，无随机失败（概率类用例带统计容差，单轮总耗时约 0.3 秒）。

---

## 5. 测试过程中发现的问题与处理

### 5.1 首轮执行：3 个概率用例失败（测试缺陷，非生产代码缺陷）

首轮结果 `Tests run: 46, Failures: 3`，失败现象：

- `advancedPoolRateByRow`：row5 高级池期望 60%，实测 **17.85%**；
- `midAdvancedPoolRatios`：大史莱姆条件占比期望 40%，实测 **0%**。

**根因**：测试用「Java 类」区分基础/高级变体，但工厂里两组变体是**同一个类的静态工厂方法**：

```java
Slime.base()  → new Slime(...)   // class = Slime，name = "史莱姆"
Slime.big()   → new Slime(...)   // class = Slime，name = "大史莱姆"
CultistPig.base()/veteran() 同理
```

按类分池时，大史莱姆（高级池 40%）和老兵邪教猪（30%）被误判为基础怪；
row5 高级池实测 17.85% ≈ 0.6 × 0.3（只剩神风猪被正确识别），与失败数值精确吻合，反向验证了生产代码的概率实现是正确的。

**处理**：按工程纪律「以生产代码为准修测试」，改为按 `enemy.name` 分池，
并在测试类 Javadoc 中固化该约定（**变体同构时只能按名字识别**）。修复后 47/47 通过，未改动任何生产代码。

### 5.2 已验证无问题的历史关注点

- 巨猪骑士削血上限可减到 0 并触发死亡：`reduceMaxHpClampsHp` 覆盖 0 边界；
- 仪式「激活后第一个玩家回合不加力量」：`ritualTiming` 逐回合验证；
- 神风猪「每次蓄势 1 层、每层 16」：`planAndPerStackDamage` 锁定轮盘数值；
- 读档刷怪防护：`eliteDeterministic` + `seedDeterminism` 验证种子绑定。

---

## 6. 概率测试的统计口径

概率类用例不是「跑一次断言」，而是固定样本量 + 容差：

| 用例 | 样本量 | 期望值 | 容差 | 对应 3σ 理论波动 |
|---|---|---|---|---|
| 基础池比例 | 6000 | 0.40/0.30/0.30 | ±0.045 | ≈ ±0.012 |
| 高级池总占比 | 6000 | 0.60 / 0.80 | ±0.04 | ≈ ±0.011 |
| 中层高级池条件分布 | 5000 | 0.40/0.30/0.30 | ±0.06 | ≈ ±0.013 |
| 后五层条件分布 | 6000 | 0.20/0.30/0.10/0.40 | ±0.05 | ≤ ±0.013 |
| 闪电猪闪避率 | 20000 | 0.40 | ±0.05 | ≈ ±0.006 |

容差均为理论波动的 3 倍以上，正常环境下不会偶发失败；如在 CI 上出现极端波动，优先重跑而非放宽容差。

---

## 7. 未覆盖部分与后续建议

| 未覆盖内容 | 原因 | 建议 |
|---|---|---|
| `BattleView` 出牌/受伤/格挡结算 | 强依赖 JavaFX 场景图与动画时钟，无头单测无法实例化 | 引入 TestFX（`testfx-junit5`）做 UI 集成测试；或把伤害结算再下沉为纯函数后补单测 |
| `BattleView` 转阶段/锁血演出时序 | Timeline/FadeTransition 驱动 | TestFX 的 `WaitForAsyncUtils` 控制动画步 |
| `CardPlay` 出牌结算 | 与 BattleView 状态耦合 | 抽出 `resolve(card, player, enemy)` 纯结算函数后可低成本补测 |
| `SaveData` 存档读写 | 涉及文件 IO | 用 `@TempDir` 做序列化→反序列化往返测试 |
| `RelicFun` 30 种遗物 | 效果分散、部分挂战斗回调 | 按「回合开始/结束/受击/战斗结束」四个钩子分组补表驱动测试 |

本轮已把**可独立测试的游戏规则核心（敌人全部机制、生成概率、玩家生命模型）**纳入回归保护，
后续调整怪物数值或概率池常量时，`mvn test` 可立即发现规则偏差。
